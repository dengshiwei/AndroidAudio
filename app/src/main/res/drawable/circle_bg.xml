<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- 填充色 -->
    <solid android:color="@color/button_gray" />

    <!-- 边框 -->
    <stroke
        android:width="1dp"
        android:color="@android:color/holo_blue_dark" />

    <!-- 大小（可选，用在直接引用 background 时控制尺寸） -->
    <size
        android:width="60dp"
        android:height="60dp" />
</shape>
